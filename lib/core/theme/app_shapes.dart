import 'package:flutter/material.dart';

class AppShapes {
  // Buttons
  static const double buttonRadius = 28.0;
  static const BorderRadius buttonBorderRadius = BorderRadius.all(Radius.circular(buttonRadius));

  // Inputs
  static const double inputRadius = 10.0;
  static const BorderRadius inputBorderRadius = BorderRadius.all(Radius.circular(inputRadius));

  // Advanced Inputs
  static const double halfWidthInputRadius = 20.0;
  static const BorderRadius halfWidthBorderRadius = BorderRadius.all(Radius.circular(halfWidthInputRadius));
  static const double checkboxRadius = 4.0;
  static const BorderRadius checkboxBorderRadius = BorderRadius.all(Radius.circular(checkboxRadius));

  // Chips & Badges
  static const double pillRadius = 20.0;
  static const BorderRadius pillBorderRadius = BorderRadius.all(Radius.circular(pillRadius));
  static const double roleBadgeRadius = 6.0;
  static const BorderRadius roleBadgeBorderRadius = BorderRadius.all(Radius.circular(roleBadgeRadius));

  // Cards
  static const double cardRadius = 10.0;
  static const BorderRadius cardBorderRadius = BorderRadius.all(Radius.circular(cardRadius));

  // Misc
  static const double imageAssetRadius = 8.0;
  static const BorderRadius imageAssetBorderRadius = BorderRadius.all(Radius.circular(imageAssetRadius));
  static const double fabRadius = 18.0;
  static const BorderRadius fabBorderRadius = BorderRadius.all(Radius.circular(fabRadius));
  static const double otpDigitBoxRadius = 4.0;
  static const BorderRadius otpDigitBorderRadius = BorderRadius.all(Radius.circular(otpDigitBoxRadius));
}
